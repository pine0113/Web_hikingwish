<%= link_to "計畫列表", plans_path + "?type=applied" ,class: "btn btn-light" %>
<%= link_to "計畫詳情", plan_path(@invite.plan) ,class: "btn btn-light" %>

<%= form_for @messenge do |f|  %>

    <%= f.hidden_field :chatable_type, value: @invite.class.name %>
    <%= f.hidden_field :chatable_id, value: @invite.id %>
    <%= f.text_field :content , class: "form-control", placeholder: "What do you want to say?" %>
    <%= f.submit '發送',class: "btn btn-primary", name: nil %>
<% end %>

<% unless @messenges.nil? %>
    <% @messenges.each do |m| %>

        <%= m.user.name %> : <%= m.content%> <%=time_ago_in_words(m.created_at)%> <br>

    <% end %> 
<% end %>
